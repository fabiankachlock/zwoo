
name: E2E-Test Pipeline
on:
  workflow_dispatch:
  # todo remove on push to every thing
  push:
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build Test Runner Image
        run:  docker build -f ./e2e/Dockerfile -t zwoo-e2e .
      - name: Run E2E-Tests
        run: docker run -it zwoo-e2e