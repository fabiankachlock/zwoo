version: '3.4'

services:
  backend:
    image: backend
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      - ZWOO_USERNAME=""
      - ZWOO_EMAIL=""
      - ZWOO_PASSWORD=""
      - ZWOO_SIDESECRET=""
      - ZWOO_PORT=8000

  volumes:
    db-data:
      driver: local
